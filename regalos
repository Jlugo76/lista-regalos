<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Regalos – Jhonathan</title>

    <!-- Tipografía estilo Apple -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap">

    <style>
        /* Fondo degradado estilo Apple */
        body {
            margin: 0;
            font-family: "Inter", -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, #0d0d0d, #1a1a1a, #111);
            color: white;
            overflow-x: hidden;
        }

        /* Menú superior */
        nav {
            display: flex;
            justify-content: center;
            gap: 40px;
            padding: 20px;
            backdrop-filter: blur(20px);
            background: rgba(0, 0, 0, 0.4);
            position: fixed;
            width: 100%;
            z-index: 10;
        }

        nav a {
            color: #ccc;
            text-decoration: none;
            font-size: 15px;
            transition: 0.3s;
        }

        nav a:hover {
            color: white;
        }

        /* Hero / portada */
        .hero {
            height: 65vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding-top: 80px;
            animation: fadeIn 2s ease;
        }

        .hero img {
            width: 280px;
            border-radius: 20px;
            box-shadow: 0 8px 30px rgba(0,0,0,0.6);
            animation: float 6s ease-in-out infinite;
        }

        .hero h1 {
            margin-top: 25px;
            font-size: 42px;
            font-weight: 600;
            letter-spacing: -1px;
        }

        .hero p {
            margin-top: 10px;
            font-size: 18px;
            color: #ccc;
        }

        /* Animación de flotación estilo Apple */
        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-12px); }
        }

        /* Fade In */
        @keyframes fadeIn {
            from { opacity: 0; }
            to   { opacity: 1; }
        }

        /* Sección del Notion */
        iframe {
            width: 100%;
            height: 100vh;
            border: none;
            margin-top: 40px;
            animation: fadeIn 1.6s ease;
        }

        footer {
            text-align: center;
            padding: 20px;
            color: #aaa;
            font-size: 14px;
            margin-top: 40px;
        }
    </style>
</head>
<body>

    <!-- Menú estilo Apple -->
    <nav>
        <a href="#inicio">Inicio</a>
        <a href="#lista">Lista</a>
        <a href="#footer">Contacto</a>
    </nav>

    <!-- Portada -->
    <section id="inicio" class="hero">
        <img src="TU_IMAGEN_DE_PORTADA.png" alt="Portada">
        <h1>Lista de Regalos</h1>
        <p>Una selección minimalista y elegante — estilo Apple 🍏</p>
    </section>

    <!-- Notion embebido -->
    <section id="lista">
        <iframe 
            src="https://secretive-balaur-8b3.notion.site/Regalos-3020a07356e2809bbd3ffb9c5ef62d4f"
            allowfullscreen
        ></iframe>
    </section>

    <footer id="footer">
        Creado por Jhonathan • Notion + GitHub Pages 🍏✨
    </footer>

</body>
</html>
